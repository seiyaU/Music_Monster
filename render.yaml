services:
  - type: web
    name: music-cat
    env: python
    plan: starter  # Use the Starter plan.
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers=2 --timeout=180
    healthCheckPath: /health
    autoDeploy: true
